# Gbedity game client — copy to .env.local and adjust as needed.
# REST base URL (includes /api/v1).
VITE_API_BASE_URL=http://localhost:8090/api/v1
# Socket.IO origin (no path).
VITE_WS_URL=http://localhost:8090
# development | production
VITE_APP_ENV=development
