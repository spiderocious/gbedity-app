import { Button } from '@gbedity/ui';

export function App() {
  return (
    <main className="app">
      <h1>Gbedity Admin</h1>
      <p>Template scaffold — nothing built yet.</p>
      <Button onClick={() => console.warn('admin button clicked')}>Get started</Button>
    </main>
  );
}
